/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['debearings.com', 'www.bultannews.com', 'tabrizpm.ir'],
  },
};

module.exports = nextConfig;
